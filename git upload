
pipeline {
    agent { label'Remote Machine' }
    options { timestamps() }
    stages {
        stage('upload') {
            steps {
                script {    bat  """echo "%computername%" """
                    powershell """ cgi c: """
                }
            }
        }
    }
        post {
            always {
                cleanWs()
            }
        }
}

