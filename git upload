
pipeline {
 
    agent { label'Remote Machine' }
options { timestamps () }
    stages {
        stage('upload') {
            steps {
                script {    bat  """ echo "%computername%" """
                }
            }
        }
    }
        post {
            always {
                cleanWs()
            }
        }
    }

