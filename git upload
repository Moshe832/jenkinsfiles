
pipeline {
 
    agent { label'Remote Machine' }
         options  timestamps {
     some block
}
    stages {
        stage('upload') {
            steps {
                script {    bat  """ echo "%computername%" """
                }
            }
        }
    }
        post {
            always {
                cleanWs()
            }
        }
    }

