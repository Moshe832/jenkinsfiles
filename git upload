
pipeline {
            timestamps {
     some block
}
    agent { label'Remote Machine' }

    stages {
        stage('upload') {
            steps {
                script {    bat  """ echo "%computername%" """
                }
            }
        }
    }
        post {
            always {
                cleanWs()
            }
        }
    }

